<Window x:Class="Nulmesis.App.Views.DeleteConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="确认删除" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 警告图标和标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="⚠️" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="即将删除文件" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 扫描信息 -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="扫描模式：" FontWeight="Bold" Margin="0,0,5,5"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Mode}" Margin="0,0,0,5"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="根目录：" FontWeight="Bold" Margin="0,0,5,5"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Root}" TextWrapping="Wrap" Margin="0,0,0,5"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="命中数量：" FontWeight="Bold" Margin="0,0,5,0"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding MatchedCount}" Foreground="Red" FontWeight="Bold"/>
        </Grid>

        <!-- 预览列表 -->
        <TextBlock Grid.Row="2" Text="前 10 条预览：" FontWeight="Bold" Margin="0,0,0,5"/>

        <ListBox Grid.Row="3" 
                 ItemsSource="{Binding PreviewPaths}"
                 BorderBrush="{x:Static SystemColors.ActiveBorderBrush}"
                 BorderThickness="1"
                 Margin="0,0,0,15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" ToolTip="{Binding}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确认删除" 
                    Click="ConfirmButton_Click"
                    Width="100"
                    Height="30"
                    Margin="0,0,10,0"
                    Background="#FFD32F2F"
                    Foreground="White"
                    FontWeight="Bold"/>
            <Button Content="取消" 
                    Click="CancelButton_Click"
                    Width="100"
                    Height="30"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>